### Q1. Merge HDR Images

``` python hdr.py ```

### Q2. Color correction
Find affine transformation matrix and do color correction using:  ``` python homography.py ```


### Q3. Tonemapping
Tonemapping function also can be done using: ``` python hdr.py ```

### Q4. Custom HDR Merge can also be done using : ``` python hdr.py ```

### Q5. Noise Calibration : ``` python normalise.py ```

